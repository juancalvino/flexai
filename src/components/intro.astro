---
import { Picture } from "astro:assets";
import motoPedido from "../assets/motoPedido.svg";
import Link from "@components/ui/link.astro";
import { Icon } from "astro-icon/components";
import { whatsappLink } from '@utils/whatsapp';
---

<main
  class="grid lg:grid-cols-2 place-items-center pt-16 pb-8 md:pt-12 md:pb-24">
  <div class="py-6 md:order-1 hidden md:block">
    <Picture
      src={motoPedido}
      alt="GPS y moto de FLEXAI"
      widths={[200, 400, 600]}
      sizes="(max-width: 800px) 100vw, 620px"
      loading="eager"
      format="avif"
    />
  </div>
  <div>
    <h1
      class="text-5xl lg:text-6xl xl:text-7xl font-bold lg:tracking-tight xl:tracking-tighter">
      La forma flex de realizar envios inteligentes
    </h1>
    <p class="text-lg mt-4 text-slate-600 max-w-xl">
      ¡Impulsa tus ventas con envíos Flex sin complicaciones!🚀 <br>
      Nos encargamos de tu logística de forma rápida y eficiente,
      para que te concentres en lo que realmente importa: hacer crecer tu negocio.📈
      Olvídate de las demoras y la complejidad, ¡nosotros te simplificamos el proceso!
    </p>
    <div class="mt-6 flex flex-col sm:flex-row gap-3">
      <Link
        href= {whatsappLink}
        target="_self"
        class="flex gap-1 items-center justify-center"
        rel="noopener">
        <Icon class="text-white w-5 h-5" name="bx:bxs-rocket" />
        Comenzar con FLEXAI
      </Link>
      <Link
        size="lg"
        style="outline"
        rel="noopener"
        href="/precios"
        class="flex gap-1 items-center justify-center"
        target="_self">
        <Icon class="text-black w-4 h-4" name="bx:bx-bar-chart-alt-2" />
         Ver precios
      </Link>
    </div>
  </div>
</main>